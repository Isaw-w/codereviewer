#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)
CODE_DIR=$(cd "$SCRIPT_DIR/.." && pwd)
python3 "$CODE_DIR/validation/validate_release_closure.py"
python3 "$CODE_DIR/validation/validate_release_metrics.py" --section all --write-expected
